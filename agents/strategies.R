#imitation, hybridA, hybridB,local search, random search


imitation <- function(fitnessMatrix, prevChoices, network, NK = FALSE){

}

hybrid <- function(fitnessMatrix, prevChoices, network, NK=FALSE, RS = 0){
	#RS  \in {0, .2)
}

indSearch <- function(fitnessMatrix, prevChoices, RS= 0, NK = FALSE){
	#RS = 0 is hill-climbing, RS = 1 is random search
}